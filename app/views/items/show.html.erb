<p style="color: green"><%= notice %></p>

<%= render @item %>

<div>
  <% if can? :manage, @item %>
    <%= link_to "Edit this item", edit_item_path(@item) %> |
    <%= button_to "Destroy this item", item_path(@item), method: :delete %>
  <% end %>

  <%= link_to "Back to items", items_path %>


</div>
